
<div class="padding">
  <%= render 'form', guide: @guide %>
<div class="tags">
    <% @tag.each do |t| %>
      <%= link_to t.name,  guide_guide_tags_path(@guide, :tag_id => t.id), method: :post, class: "tag" %>



    <% end %>
  </div>
</div>

